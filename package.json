{
  "name": "ahmed-fayyaz-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --names API,WEB --prefix-colors green,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently --kill-others --names API,WEB --prefix-colors green,cyan \"npm --prefix backend start\" \"npm --prefix frontend start\"",
    "build": "npm --prefix frontend run build",
    "seed": "npm --prefix backend run seed",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}

